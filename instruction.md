Amboy 的 MongoDB 远程队列会定期刷新作业锁，但续租失败只记录错误，旧执行者仍可能产生副作用并进入完成、重试或作用域释放流程。请为内置远程执行建立端到端的租约隔离生命周期，使每次成功派发都携带可验证的租约代次并贯穿作业执行上下文、状态保存、完成、重试和作用域变更，对短暂存储故障提供不超过锁有效期的可配置容错窗口，对所有权或代次冲突立即取消旧执行并禁止其后续写入，同时保留足够的续租与失租状态供诊断，让其他实例可以安全接管，且正常续租、队列关闭和本地队列行为保持兼容。
